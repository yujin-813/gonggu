# 꿀공구 — AI 작업 지침

> 자세한 내용은 `docs/01-CONSTITUTION.md`(원칙) · `docs/02-DECISIONS.md`(결정) · `docs/03-SYSTEM.md`(현재) · `docs/GLOSSARY.md`(용어).
> 이 파일은 **코드를 쓸 때 실제로 필요한 것만** 압축한 것이다.

---

## 제품 한 문장

**꿀공구는 인스타그램 공동구매가 진짜 싼지 판정해주는 서비스다.** 공구를 모아 보여주는 건 판정을 붙이기 위한 준비다.

### 판단 기준

무언가를 정할 때 이 순서로 본다.

1. **틀린 숫자를 내보내는가?** — 가격·할인율이 틀리면 그 한 번으로 신뢰가 끝난다. 다른 모든 것보다 우선한다.
2. **확신이 없는데 말하고 있는가?** — 모르면 말하지 않는다. "판정 불가"가 틀린 판정보다 낫다.
3. **고객이 읽고 판단할 수 있는가?** — 우리 사정(검수 상태, 수집 실패)을 고객 화면에 쓰지 않는다.
4. **되돌릴 수 있는가?** — 되돌리기 쉬우면 그냥 한다.

---

## 하지 않기로 한 것 (제안하지 마세요)

| | 이유 |
|---|---|
| 사이트 내 결제 | 모든 구매는 외부몰에서. 정산·환불·CS가 따라온다 |
| 남의 쇼핑몰 장바구니에 담기 | 아임웹 2단계 옵션 가격을 얻으려 해도 하지 않는다. 선을 넘는 동작 |
| 사이트별 크롤러 40종 | 도메인이 흩어져 있다. 솔루션 단위(위즈·카페24·아임웹)로만 대응하고 나머지는 붙여넣기 |
| 댓글 기능 | DAU 300 전까지 보류 |
| 키워드 알림 | DAU 100 전까지 보류 (현재 푸시 구독자 2명) |
| PM2 cluster 다중 인스턴스 | 서버 메모리 1.9GB. 빌드와 겹치면 OOM |
| 외부 분석 도구 도입 | 자체 analytics가 이미 있다 |
| 네이버 쇼핑 API 복구 시도 | **API가 폐지됐다.** 앱 권한 문제가 아니라 되살릴 수 없다 (`D-019`) |
| 네이버·쿠팡 웹페이지 스크래핑 | 429로 하드 차단. 헤더를 갖춰도 막힌다 |

---

## 이미 결정된 것 (다시 제안하지 마세요)

| 주제 | 결론 | 근거 |
|---|---|---|
| 등급 체계 | 4단계 고정 (꿀딜 15%↑ / 괜찮딜 5~15% / 고민딜 ±5% / 아쉽딜 −5%↓) | `D-004` |
| 판정 불가 표시 | 등급이 아닌 **별도 상태**(`pending`/`exclusive`/`multi`) | `D-005` |
| 판정 대기 배지 | 목록에서 **안 보여준다.** 상세에서만 설명 | `D-018` |
| 옵션 등급 산출 | **중앙값** (최댓값·평균 아님) | `D-008` |
| 자동 매칭 신뢰 | 공구가의 50% 미만이면 버린다 (`AUTO_MATCH_FLOOR`) | `D-006` |
| 자동 수집 옵션 | 대표가를 **낮추지 않는다.** 구성 목록으로만 | `D-011` |
| 카드 할인율 | `getDealVerdict()`에서만. 컴포넌트가 계산하지 않는다 | `D-014` |
| 제휴 링크 vs 비교가 | 필드를 **분리**한다 (공정위 고지 때문) | `D-003` |
| 마감 공구 상세 | URL을 **살려둔다** (`isPagePublic`) | `D-002` |
| 색 사용 | 주황=꿀딜·CTA / 빨강=오늘·내일 마감 / 초록=혜택 | `D-017` |
| CTA 버튼 | 채운 주황 유지 (테두리형은 시도 후 되돌림) | `D-017` |
| 배포 | 별도 디렉터리 빌드 후 교체 + 자동 롤백 | `D-015` |
| 유입 경로 | utm → 리퍼러 → UA 인앱 표식 순, 첫 진입 시점 고정 | `D-016` |
| 비교가 출처 | 네이버 쇼핑 API는 폐지됨. 쿠팡 파트너스 검토 중 | `D-019` |
| 조용한 고장 | 판정 없이 고객에게 보이는 공구가 10건 넘으면 관리자 화면에 경고 | `D-025` |
| 관리자 제외 | 쿠키 + 흔적 쿠키 + IP 세 겹 | `D-007` |
| 소유확인 코드 | 교체하지 않고 쉼표로 여러 개 | `D-021` |
| 문서 검증 | `03-SYSTEM.md`의 숫자를 스크립트가 다시 잰다 | `D-022` |
| 비교 상태 | 미확인/비교가 있음/비교불가 3분류. **관리자 전용**이고 비교불가는 사람만 고른다 | `D-023` |
| 동일상품 후보 | provider 목록으로 분리. 자동 적용 안 함 — 관리자가 고를 때만 저장 | `D-023` |
| 마감일 미확인 | 상시딜과 **다른 상태**. 시작일(없으면 수집일)+21일 지나면 마감 처리 | `D-024` |

---

## 용어 (자주 틀리는 것만)

| 화면 | 코드 |
|---|---|
| 공구가(대표가, 최저가 아님) | `post.price` |
| 정가 (사람이 입력) | `post.origPrice` |
| 네이버 최저가 (자동) | `post.market_price` |
| 구성 / 세트 | `post.options[]` |
| 대체 구매 링크 (제휴 고지 대상) | `post.purchase_links[]` |
| 가격 비교 전 | `pending` |

- `CompareState`(관리자 작업 상태) ≠ `VerdictState`(고객이 보는 판정)
- `deadline_unknown`(마감일을 모른다) ≠ `evergreen`(사람이 상시딜이라고 확인했다)

**헷갈리는 쌍**
- `status` ≠ `published` — 공개 판정은 `published`가 한다
- `origPrice`(사람) ≠ `market_price`(자동) — 자동값을 `origPrice`에 넣지 말 것
- `purchase_links`(제휴, 고지 붙음) ≠ `market_url`(비교, 고지 없음)
- `isCustomerVisible`(목록, 마감 제외) ≠ `isPagePublic`(상세, 마감 포함)
- `purchase_url`(구매 페이지) ≠ `url`(인스타 프로필)

전체는 `docs/GLOSSARY.md`.

---

## 기술 스택 / 구조

- Next.js 14 App Router · React 18 · TypeScript · lucide-react
- **저장소는 파일** — `data/*.json`. DB 없음
- 수집기는 파이썬 (`collector.py` → `inpock.py`), cron 하루 2회
- 배포: EC2 + PM2(fork, :3002) + nginx, `bash deploy.sh`

```
app/          라우트 (page.tsx / api/*/route.ts)
components/   화면 조각 (PostCard, DealStrip, DealVerdictBox, AddPostModal …)
lib/          도메인 로직 — 여기가 핵심
  dealGrade.ts    판정 (화면의 모든 숫자가 여기서 나온다)
  period.ts       기간·마감 판단
  landing.ts      홈/랜딩 섹션 계산  ※ fs를 읽는다 — 클라이언트에서 import 금지
  store.ts        파일 I/O           ※ 같음
  analytics.ts    방문·클릭·유입 경로
  optionScrape.ts 옵션 자동수집 (TS) — inpock.py와 규칙을 맞출 것
  postGuards.ts   저장 시 강제 규칙
  types.ts        타입 + CATEGORY_KEYS (클라이언트 안전)
*.py          수집기·백필
```

---

## 손대면 위험한 곳

| 위치 | 위험 |
|---|---|
| `lib/store.ts` `savePosts()` | 배열을 통째로 덮어쓴다. **락이 없다** — 동시 저장 시 유실 |
| `lib/dealGrade.ts` | 화면의 모든 숫자의 출처. 고치면 카드·상세·공유카드·정렬이 전부 바뀐다 |
| `app/api/posts/[id]/route.ts` allowlist | 여기 없는 필드는 저장해도 **조용히 무시된다.** PATCH·PUT 양쪽에 추가할 것 |
| `middleware.ts` | 새 관리자 API는 `isProtected()`와 `config.matcher` **양쪽**에 등록 |
| 클라이언트 컴포넌트의 import | `lib/landing`·`lib/store`를 import하면 `fs` 때문에 **전 페이지 500** |
| 마감일 형식 | `YYYY-MM-DD` 문자열 비교. 시각이 섞이면 표시·비교가 동시에 깨짐 |
| `AUTO_MATCH_FLOOR` | 낮추면 잘못된 자동 매칭이 판정을 뒤집는다 |

---

## 코드 작성 규칙

- **주석은 "왜"만 쓴다.** 무엇을 하는지는 코드가 말한다. 특히 우회·예외·안전장치는 이유를 반드시 남긴다 (기존 코드가 그렇게 돼 있다)
- 주변 코드의 밀도·명명·관용구에 맞춘다
- 화면 문구는 고객 언어로. 내부 상태명을 그대로 쓰지 않는다
- 새 색을 쓰기 전에 `D-017`의 색 규칙을 먼저 본다
- 파이썬과 TS에 같은 규칙이 있으면(옵션 파서, 구매링크 필수, `isCustomerVisible`/`DEADLINE_UNKNOWN_DAYS`) **양쪽을 함께 고친다**

---

## 나와 일하는 방식

**먼저 상의할 것** (되돌리기 어려움)
- 데이터 스키마 변경, 인증·권한, 핵심 enum(`status`, `VerdictState`, `Category`) 추가·변경
- 운영 데이터를 바꾸는 스크립트 실행
- 제품 방향에 관한 판단 (무엇을 만들지, 무엇을 뺄지)

**묻지 말고 진행할 것** (되돌리기 쉬움)
- 버그 수정, 문구·색·여백, 리팩터링, 테스트·검증 스크립트

**기준으로 판단이 안 되면 추측하지 말고 질문한다.** 특히 사실 확인이 필요한 것을 지어내지 않는다. 모르면 `⚠️`로 표시한다.

**작업 방식**
- 로컬에서 검증을 끝내고 **여러 변경을 모아 한 번에 배포**한다. 변경마다 배포하지 않는다
- 데이터를 건드리는 스크립트는 `--dry-run` 먼저, 백업 후 실행
- 사실을 주장하기 전에 실제로 재본다. 추정이면 추정이라고 말한다

---

## 검증 명령어

```bash
npx tsc --noEmit                                  # 타입
npm run build                                     # 빌드
node scripts/check-docs.js                        # docs/03-SYSTEM.md의 숫자가 아직 맞는지
node node_modules/.bin/next dev -p 3210           # 로컬 (3000·3100은 다른 프로젝트가 쓸 수 있음)
```

배포:
```bash
git push origin main
ssh -i ~/.ssh/gonggu_ec2 ubuntu@13.125.121.62 'cd ~/gonggu && bash deploy.sh'
```

서버에서 파이썬은 `venv/bin/python`. 긴 작업은 `nohup ... &`로 분리 실행(SSH 끊기면 죽는다).

---

## 작업이 끝나면

매번 다음을 **먼저 제안**한다.

1. `docs/02-DECISIONS.md`에 남길 결정이 있는지 — **버린 대안과 받아들인 대가가 없으면 항목을 만들지 않는다.** 되돌리기 쉽고 영향 좁은 것(문구·색·변수명)은 기록하지 않는다. 기존 결정을 뒤집었으면 옛 항목을 고치지 말고 새 항목을 만든 뒤 옛 항목에 "→ D-0XX로 뒤집힘" 한 줄만 덧붙인다
2. `docs/03-SYSTEM.md`에서 이번 작업으로 **사실과 달라진 부분** — 이력을 추가하지 말고 덮어쓴다. "지금"만 남아야 한다
3. `docs/GLOSSARY.md`에 새 용어
4. 이 파일(`CLAUDE.md`)의 "이미 결정된 것" / "하지 않기로 한 것"이 바뀌었는지

고칠 게 없으면 "없음"이라고만 답한다. 억지로 만들지 않는다.
